services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jurishop
    volumes:
      - jurishop_pg:/var/lib/postgresql/data

volumes:
  jurishop_pg:
